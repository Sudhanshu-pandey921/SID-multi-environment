trigger:
  branches:
    include:
      - main
      - develop
      - feature/*
  paths:
    include:
      - SID-multi-environment/**
    exclude:
      - README.md
      - docs/**

pr:
  branches:
    include:
      - main
      - develop
  paths:
    include:
      - SID-multi-environment/**
    exclude:
      - docs/**

parameters:
  - name: environment
    type: string
    default: dev
    values:
      - dev
      - stage
      - prod

variables:
  - name: tfVersion
    value: '1.5.7'

  - name: serviceConnection
    value: 'may-sc'   # ✅ FIXED

  - name: workingDir
    value: 'terraform/${{ parameters.environment }}'

  - name: backendResourceGroup
    value: 'tf-backend-rg'

  - name: backendStorageAccount
    value: 'tfbackendstorage123'

  - name: backendContainer
    value: 'tfstate'

  - name: backendKey
    value: '${{ parameters.environment }}.tfstate'

pool:
  vmImage: ubuntu-latest   # ✅ Linux use kiya (curl scripts ke liye)

stages:

# ---------------------------
# 🔍 VALIDATION + LINT + SECURITY
# ---------------------------
- stage: Validate
  displayName: "Lint, Security & Validate"
  jobs:
    - job: ValidateJob
      steps:

        - task: TerraformInstaller@1
          inputs:
            terraformVersion: $(tfVersion)

        - script: terraform init -backend=false
          workingDirectory: $(workingDir)
          displayName: "Terraform Init (Local)"

        - script: terraform fmt -check -recursive
          workingDirectory: $(workingDir)
          displayName: "Terraform Format Check"

        - script: |
            curl -s https://raw.githubusercontent.com/terraform-linters/tflint/master/install_linux.sh | bash
            tflint
          workingDirectory: $(workingDir)
          displayName: "Run TFLint"

        - script: |
            curl -s https://raw.githubusercontent.com/aquasecurity/tfsec/master/scripts/install_linux.sh | bash
            tfsec .
          workingDirectory: $(workingDir)
          displayName: "Run TFSEC"

        - script: terraform validate
          workingDirectory: $(workingDir)
          displayName: "Terraform Validate"


# ---------------------------
# 💰 COST ESTIMATION
# ---------------------------
- stage: CostEstimation
  displayName: "Cost Estimation"
  dependsOn: Validate
  jobs:
    - job: CostJob
      steps:
        - script: |
            terraform init -backend=false
            curl -fsSL https://raw.githubusercontent.com/infracost/infracost/master/scripts/install.sh | sh
            infracost breakdown --path $(workingDir)
          workingDirectory: $(workingDir)
          displayName: "Run Infracost"


# ---------------------------
# 📦 PLAN
# ---------------------------
- stage: Plan
  displayName: "Terraform Plan"
  dependsOn: CostEstimation
  jobs:
    - job: PlanJob
      steps:

        - task: TerraformInstaller@1
          inputs:
            terraformVersion: $(tfVersion)

        - task: AzureCLI@2
          displayName: "Terraform Plan"
          inputs:
            azureSubscription: $(serviceConnection)
            scriptType: bash
            scriptLocation: inlineScript
            inlineScript: |
              terraform init \
                -backend-config="resource_group_name=$(backendResourceGroup)" \
                -backend-config="storage_account_name=$(backendStorageAccount)" \
                -backend-config="container_name=$(backendContainer)" \
                -backend-config="key=$(backendKey)"

              terraform plan -out=tfplan
            workingDirectory: $(workingDir)

        - publish: $(workingDir)/tfplan
          artifact: tfplan


# ---------------------------
# 🚀 APPLY (DEV / STAGE auto)
# ---------------------------
- stage: Apply_NonProd
  displayName: "Apply Dev/Stage"
  dependsOn: Plan
  condition: and(succeeded(), ne('${{ parameters.environment }}', 'prod'))
  jobs:
    - deployment: ApplyJob
      environment: ${{ parameters.environment }}
      strategy:
        runOnce:
          deploy:
            steps:

              - download: current
                artifact: tfplan

              - task: TerraformInstaller@1
                inputs:
                  terraformVersion: $(tfVersion)

              - task: AzureCLI@2
                displayName: "Terraform Apply NonProd"
                inputs:
                  azureSubscription: $(serviceConnection)
                  scriptType: bash
                  scriptLocation: inlineScript
                  inlineScript: |
                    terraform init \
                      -backend-config="resource_group_name=$(backendResourceGroup)" \
                      -backend-config="storage_account_name=$(backendStorageAccount)" \
                      -backend-config="container_name=$(backendContainer)" \
                      -backend-config="key=$(backendKey)"

                    terraform apply -auto-approve tfplan
                  workingDirectory: $(workingDir)


# ---------------------------
# 🛑 PRODUCTION APPROVAL + APPLY
# ---------------------------
- stage: Apply_Prod
  displayName: "Apply Production"
  dependsOn: Plan
  condition: and(succeeded(), eq('${{ parameters.environment }}', 'prod'))

  jobs:
    - deployment: ProdDeployment
      environment: prod
      strategy:
        runOnce:
          deploy:
            steps:

              - download: current
                artifact: tfplan

              - task: TerraformInstaller@1
                inputs:
                  terraformVersion: $(tfVersion)

              - task: AzureCLI@2
                displayName: "Terraform Apply Prod"
                inputs:
                  azureSubscription: $(serviceConnection)
                  scriptType: bash
                  scriptLocation: inlineScript
                  inlineScript: |
                    terraform init \
                      -backend-config="resource_group_name=$(backendResourceGroup)" \
                      -backend-config="storage_account_name=$(backendStorageAccount)" \
                      -backend-config="container_name=$(backendContainer)" \
                      -backend-config="key=$(backendKey)"

                    terraform apply tfplan
                  workingDirectory: $(workingDir)